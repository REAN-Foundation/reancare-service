export enum SupportedLanguage {
    English = 'en-US',
    Spanish = 'es-ES',
}

export enum InactiveUsersNotificationFrequency {
        FifteenDays = 15,
        OneMonth = 30,
}

export enum InactiveUserDuration {
    OneMonth = 30,
    ThreeMonths = 90,
    SixMonths = 180,
    OneYear = 365,
}
